apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "second-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "second-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "second-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "second-deploy.fullname" . }}
  labels:
    {{- include "second-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "second-deploy.selectorLabels" . | nindent 4 }}

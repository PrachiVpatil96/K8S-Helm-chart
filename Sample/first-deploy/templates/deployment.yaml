---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Values.name1 }}
  labels:
    app: {{.Values.app}}
spec:
  minReadySeconds: {{.Values.minReadySeconds }}
  replicas: {{.Values.replicas}}
  selector:
    matchLabels:
      app: {{.Values.app}}
  template:
    metadata:
      labels:
        app: {{.Values.app}}
    spec:
        containers:
          - name: {{.Values.name2}}
            image: {{.Values.image}}
            ports:
              - containerPort: {{.Values.containerPort}}
            resources:
              requests:
                cpu: 300m
                memory: 64Mi
              limits:
                cpu: 350m
                memory: 128Mi  